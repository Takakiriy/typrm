Target of template file >> multi_paths >> OK:
    multi_paths OK first
    second
    third
Target of template file >> multi_paths >> NG:
    multi_paths NG first
    second BAD
    third
Target of template file >> exist_if:
    exist_if first
    second BAD
    third
